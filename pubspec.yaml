name: project_micah
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  stacked: ^3.4.0
  stacked_services: ^1.1.0
  responsive_builder: ^0.7.0
  flutter_animate: ^4.1.1+1
  url_strategy: ^0.3.0
  intl: ^0.19.0
  google_fonts: ^6.1.0
  flutter_breadcrumb: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6
  flutter_lints: ^6.0.0
  mockito: ^5.4.1
  stacked_generator: ^2.0.0
  json_serializable: ^6.9.0
  golden_toolkit: ^0.15.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/sample_mcs/
    - assets/images/sample_parts/
    - assets/images/Others/
    - assets/data/

